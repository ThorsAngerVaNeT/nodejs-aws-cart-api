# would be generated by npm inside docker
node_modules

# would be builded by npm script inside docker
dist

# no need to have tests inside production image
test

# no need to have Dockerfile and .dockerignore inside image
Dockerfile
.dockerignore

# no need to have git repo inside image
.git

# no need to have docs inside image
*.md

# no need to have elasticbeanstalk config inside image
.elasticbeanstalk